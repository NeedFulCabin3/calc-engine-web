# calc-engine-web
A client-side arithmetic evaluation interface featuring safe string-expression parsing via isolated JS constructors, dynamic DOM state management, operator sanitization, and warm palette styling. 
